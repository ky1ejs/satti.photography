type Customer = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  dogsName: string;
  dogsBreed: string;
  dogsAge: string;
  message: string;
};

export default Customer;
